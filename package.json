{
  "name": "communityfix",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm:dev --prefix server\" \"npm:dev --prefix client\"",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  },
  "dependencies": {
    "cloudinary": "^2.8.0",
    "multer": "^2.0.2"
  }
}
